name: Fathom

options:
  bundleIdPrefix: com.exhibinaut
  deploymentTarget:
    macOS: "14.0"
  createIntermediateGroups: true

packages:
  FathomCore:
    path: .

settings:
  base:
    SWIFT_VERSION: "6.0"
    MACOSX_DEPLOYMENT_TARGET: "14.0"
    ARCHS: arm64

targets:
  Fathom:
    type: application
    platform: macOS
    sources:
      - path: Fathom
        excludes:
          - Resources
      - path: Fathom/Resources/Assets.xcassets
        buildPhase: resources
      - path: Fathom/Resources/Fonts
        buildPhase: resources
      - path: Fathom/Resources/CountryFlags
        type: folder
      - path: Fathom/Resources/NavIcons
        type: folder
        buildPhase: resources
    dependencies:
      - package: FathomCore
        product: FathomKit
      # With no `copy:` block XcodeGen renders this as a Copy Bundle Resources
      # entry, which put the helper at Contents/Resources/FATHOM Bar.app.
      # codesign treats anything under Resources as data: measured on a
      # two-level bundle whose inner .app had its signature removed outright,
      # `codesign --verify --deep --strict --verbose=4` on the outer app still
      # reported "valid on disk / satisfies its Designated Requirement" and
      # exited 0. Contents/Library/LoginItems is the documented home for a
      # ServiceManagement-managed helper app and is a location codesign and
      # Gatekeeper both recognise as nested code.
      #
      # The LaunchAgent plist does NOT move with it. It stays at
      # Contents/Library/LaunchAgents, where SMAppService.agent(plistName:)
      # looks for it — see the postBuildScript below. Only the BundleProgram
      # string inside that plist points at the new location.
      - target: FathomBar
        copy:
          destination: wrapper
          subpath: Contents/Library/LoginItems
    preBuildScripts:
      # The asset-catalog artwork is stored in Git LFS. An unresolved pointer is
      # a readable file, so actool compiles it without complaint and the app
      # ships with 132 bytes of text as its icon. Fail before that can happen.
      - name: Verify app icons are not Git LFS pointers
        basedOnDependencyAnalysis: false
        script: |
          set -eu
          iconset="${SRCROOT}/Fathom/Resources/Assets.xcassets/AppIcon.appiconset"
          checked=0
          for icon in "$iconset"/*.png; do
            [ -f "$icon" ] || continue
            checked=$((checked + 1))
            case "$(head -c 64 -- "$icon")" in
              *git-lfs.github.com*)
                echo "error: $icon is an unresolved Git LFS pointer. Run: git lfs checkout"
                exit 1
                ;;
              *)
                ;;
            esac
          done
          if [ "$checked" -eq 0 ]; then
            echo "error: no app icons found in $iconset"
            exit 1
          fi
    postBuildScripts:
      - name: Embed menu bar agent
        script: |
          set -eu
          agents="${TARGET_BUILD_DIR}/${CONTENTS_FOLDER_PATH}/Library/LaunchAgents"
          mkdir -p "$agents"
          cp "${SRCROOT}/FathomBar/LaunchAgent/com.exhibinaut.fathom.bar.plist" "$agents/com.exhibinaut.fathom.bar.plist"
        inputFiles:
          - $(SRCROOT)/FathomBar/LaunchAgent/com.exhibinaut.fathom.bar.plist
        outputFiles:
          - $(TARGET_BUILD_DIR)/$(CONTENTS_FOLDER_PATH)/Library/LaunchAgents/com.exhibinaut.fathom.bar.plist
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.exhibinaut.fathom
        PRODUCT_NAME: FATHOM
        GENERATE_INFOPLIST_FILE: true
        INFOPLIST_KEY_CFBundleDisplayName: FATHOM
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.utilities
        INFOPLIST_KEY_LSMinimumSystemVersion: "14.0"
        INFOPLIST_KEY_NSHighResolutionCapable: true
        INFOPLIST_KEY_NSLocationUsageDescription: FATHOM uses location only when you ask to read the connected Wi-Fi network name.
        INFOPLIST_KEY_NSBluetoothAlwaysUsageDescription: FATHOM reads paired Bluetooth devices only to show their connection state and, where the device reports it, its battery level.
        CODE_SIGN_ENTITLEMENTS: Fathom/App/Fathom.entitlements
        ENABLE_HARDENED_RUNTIME: true
        ENABLE_APP_SANDBOX: false
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    scheme:
      testTargets: []
  FathomBar:
    type: application
    platform: macOS
    sources:
      - path: FathomBar
        excludes:
          - LaunchAgent
    dependencies:
      - package: FathomCore
        product: FathomKit
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.exhibinaut.fathom.bar
        PRODUCT_NAME: FATHOM Bar
        GENERATE_INFOPLIST_FILE: true
        INFOPLIST_KEY_CFBundleDisplayName: FATHOM Bar
        INFOPLIST_KEY_LSUIElement: true
        INFOPLIST_KEY_LSMinimumSystemVersion: "14.0"
        ENABLE_HARDENED_RUNTIME: true
        ENABLE_APP_SANDBOX: false
    scheme:
      testTargets: []
